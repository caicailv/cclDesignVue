name: debugger

on:
  workflow_dispatch:
  
jobs:
  publish:
    runs-on: ubuntu-latest
    steps:

      - name: Debug
        env:
          NODE_AUTH_TOKEN: ${{ secrets.NPM_TOK }}
        run: echo $NODE_AUTH_TOKEN    
